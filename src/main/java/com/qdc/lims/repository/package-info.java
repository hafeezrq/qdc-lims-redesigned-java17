/**
 * Spring Data JPA repositories for querying and persisting entities.
 * <p>
 * Repositories are the primary persistence gateway used by the service layer
 * and, in some cases, directly by desktop controllers.
 */
package com.qdc.lims.repository;
